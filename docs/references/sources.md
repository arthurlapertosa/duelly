# Sources

Sources used to guide this bootstrap:

- OpenAI, “Harness engineering”. Applied to: short `AGENTS.md`, docs as the project record, versioned tools, feedback loops, PRs, and validation.
- OpenAI Developers, “Subagents – Codex”. Applied to: `.codex/agents/*.toml`, specialist subagents, `name`, `description`, `developer_instructions`, `model_reasoning_effort`, sandboxing, and specialization rules.
- Claude Code documentation, custom subagents. Applied to: `.claude/agents/*.md`, YAML frontmatter, `model`, `effort`, `isolation: worktree`, tools, and clear descriptions.
- Polymarket documentation, contracts. Applied to: configurable Polygon Conditional Tokens contract reference.
- GitHub repository `arthurlapertosa/duelly`. Applied to: repository name, URL, and first-commit target.

Update this file whenever an operational source changes.

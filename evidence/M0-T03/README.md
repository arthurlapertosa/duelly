# M0.T03 Evidence

- `worktree-flow.log`: dry-run worktree creation, real smoke worktree creation, PR body rendering, granular commit helper, Draft PR dry-run, and cleanup.
- `rendered-pr-body.md`: generated PR body from the smoke worktree.
- `cleanup-status.txt`: final worktree, branch, and status output after cleanup.
